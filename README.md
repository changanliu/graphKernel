distribution.cpp is for distributional random walk kernel query

singlesource.cpp is for single source random walk kernel query

The Facebook graph is uploaded to support the computing of kernels on the product graph Facebook \otimes Facebook
